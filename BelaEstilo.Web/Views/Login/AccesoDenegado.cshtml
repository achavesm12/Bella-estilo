@{
    ViewData["Title"] = "Acceso Denegado";
}

<div class="container text-center mt-5">
    <div class="row justify-content-center">
        <div class="col-md-8 col-lg-6">
            <div class="p-4 shadow rounded-4 border border-danger-subtle">
                <h2 class="text-danger mb-3"><i class="bi bi-shield-lock-fill"></i> Acceso denegado</h2>
                <p class="lead">No tenés permiso para acceder a esta sección.</p>
                <a asp-controller="Home" asp-action="Index" class="btn btn-secondary mt-3">Volver al inicio</a>
            </div>
        </div>
    </div>
</div>
